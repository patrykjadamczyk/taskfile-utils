# yaml-language-server: $schema=https://taskfile.dev/schema.json
---
version: '3'

includes:
  Lyml:
    taskfile: Taskfile.local.yml
    optional: true
    aliases: ["local"]
  Lyaml:
    taskfile: Taskfile.local.yaml
    optional: true
    aliases: ["local"]
  lyml:
    taskfile: taskfile.local.yml
    optional: true
    aliases: ["local"]
  lyaml:
    taskfile: taskfile.local.yaml
    optional: true
    aliases: ["local"]
  example:
    taskfile: Taskfile.d/example.yaml
    optional: true
