# yaml-language-server: $schema=https://taskfile.dev/schema.json
---
version: '3'

tasks:
    default:
        desc: ""
        deps:
            - task1
    task1:
        desc: ""
        cmds:
            - something {{.CLI_ARGS}}
