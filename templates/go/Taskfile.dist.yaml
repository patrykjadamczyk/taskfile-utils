# yaml-language-server: $schema=https://taskfile.dev/schema.json
---
version: '3'

includes:
  Lyml:
    taskfile: Taskfile.local.yml
    optional: true
    aliases: ["local"]
  Lyaml:
    taskfile: Taskfile.local.yaml
    optional: true
    aliases: ["local"]
  lyml:
    taskfile: taskfile.local.yml
    optional: true
    aliases: ["local"]
  lyaml:
    taskfile: taskfile.local.yaml
    optional: true
    aliases: ["local"]
  format:
    taskfile: Taskfile.d/format.yaml
    optional: true
  profile:
    taskfile: Taskfile.d/profile.yaml
    optional: true
  test:
    taskfile: Taskfile.d/test.yaml
    optional: true
  build:
    taskfile: Taskfile.d/build.yaml
    optional: true
  run:
    taskfile: Taskfile.d/run.yaml
    optional: true
  deps:
    taskfile: Taskfile.d/deps.yaml
    optional: true
  check:
    taskfile: Taskfile.d/check.yaml
    optional: true
